import java.util.Scanner;

public class TempConversionProgram{
    public static void main(String[] args]{
      //  Scanner scanner = new Scanner(System.in);

       // double temp, newTemp;
       // String unit;

       // System.out.print("Enter the temperature: ");
       // temp = scanner.nextDouble;

       // System.out.print(" Convert to Celsious or fahrenheight? (C or F)");
       // unit = scanner.nextLine();

       //(unit.toUpper().equals("C")) ? newTemp = ((temp - 32)* (5/9)) : newTemp =  ((temp * 5/9) + 32);
       //System.out.printf("Converted Temperature = %,.2f %s", newTemp, string);



        //scanner.close();

    }
}