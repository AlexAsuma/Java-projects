public class stringMethods {
    public static void main(String[] args) {
        //String name = "Alex Asuma";

        // int length = name.length();
        // char letter = name.charAt(0);
        // int index = name.indexOf(" ");
        // int lastIndex = name.lastIndexOf("e");

        // name = name.toUpperCase();
        // name = name.toLowerCase();
        // name = name.trim(); //removes the whitespaces before and after
        // name = name.replace("a", "0");

        // String method to check whether thhe userinput is empty or not
        /*
         * if(nmae.isEmpty()){
         * System.out.println("Your name is Empty");
         * 
         * }
         * else{
         * System.out.println("Hello " + name);
         * }
         */

        // String method to check if any white spaces are present in the input
        /*
         * 
         * if (name.contains("")) {
         * System.out.println("Your name contains a space");
         * } else {
         * System.out.println("Your name does not contain any spaces");
         * }
         * 
         * System.out.println(name);
         */

        // String method to check if two strings are equal
        // to ignore case sensitivity use name.equalsIgnoreCase
        /*
         * if(name.equals("password")){
         * System.out.println("Your name can't be password");
         * }
         * else{
         * System.out.println("Hello " + name);
         * }
         */

        
    }

}
